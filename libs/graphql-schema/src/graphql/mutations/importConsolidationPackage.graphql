mutation ImportConsolidationPackage(
  $input: ImportConsolidationPackageMutationInput!
) {
  importConsolidationPackage(input: $input) {
    clientMutationId
    userErrors {
      field
      message
    }
  }
}
